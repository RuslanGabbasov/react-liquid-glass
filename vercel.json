{
  "buildCommand": "npm run build:demo",
  "outputDirectory": "dist-demo",
  "devCommand": "vite --config vite.config.demo.ts",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}